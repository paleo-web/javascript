// lastindex and indexof
// indexof uses for frist words 
// lastindex uses for last words

//  sarfaraz ad sarwar 
// indexod(take frist sarfaraz)
// and lastindex uses for (last sarwar )

// let text = `You already know a way to find the banned segment and replace it. Suppose the
// paragraph above has been assigned to the variable text`
 
// let renew = text.lastIndexOf("You")

// console.log(renew)

// replaced_text= text.replace(/ You ,find/gi, "We, kind")
//  variable = replaced_text.replace("we ,kind")

// console.log(replaced_text);

// ************** replace *************

// let text = `You already know a way to find the banned segment and replace it. Suppose the
// paragraph above has been assigned to the variable text`

// let renew = text.replace("know","is")

// for (let i=0; i < text.length; i++){
// if(renew.replace("You , bike")){
// }
// }

// console.log(renew)

let add = "hakjhioahiuhsuidujkd"
add.indexOf("s")
console.log(add);